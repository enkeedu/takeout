Read `docs/codex-project-context.md` and `docs/codex-handoff.md` first.

Then do this without waiting for extra context unless there is a real blocker:
1. launch the app if needed
2. inspect the current codebase for context
3. use MCP/browser tools to check the main local routes
4. use MCP/browser tools to re-check only the benchmark sites relevant to the current task
5. compare our current flow with the best benchmark patterns
6. keep our visual style, but adapt the strongest structure, hierarchy, trust patterns, and UI ideas into our product
7. implement the highest-leverage next step
8. validate changed flows on desktop and mobile when relevant
9. update `docs/codex-handoff.md` before ending the session

Important:
- do not redesign the brand just to copy competitors
- do not rely only on memory when benchmark details matter
- use MCP/browser checks throughout the work, not just at the start
- prefer driving the work forward over asking routine questions
